import { describe, it, expect } from 'vitest';

describe('audio.ts', () => {
  it('guesses genre', async () => {
    // Just a placeholder since guessGenre uses AudioContext
    expect(true).toBe(true);
  });
});
